<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juiceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewInquiryActivity"></activity>
        <activity android:name=".InquiryMainActivity" />
        <activity android:name=".IngrediantViewActivity" />
        <activity android:name=".IngrediantMainActivity" />
        <activity android:name=".AdminWelcomeActivity" />
        <activity android:name=".AdminMainActivity2" />
        <activity android:name=".AdminMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminEditProfileActivity" />
        <activity android:name=".PeekProductActivity" />
        <activity android:name=".ViewProducts" />
        <activity android:name=".AdminHome_Dashboard" />
        <activity android:name=".InsertProduct" />
    </application>

</manifest>